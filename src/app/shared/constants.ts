import {environment} from "../../environments/environment";

export const HISTORY_KEY = 'HISTORY';
export const BOOKMARKS_KEY = 'BOOKMARKS'

export const HISTORY_ENDPOINT = environment.API_URL + '/api/history';

export const BOOKMARK_ENDPOINT = environment.API_URL + '/api/bookmark';
